/** Node half of the installable DSH bundle. The visual feature runs in the client half. */
export const name = 'dsh-nba-pets'

/** Keep a normal Cordis plugin row so the package participates in the profile lifecycle. */
export function apply(): void {}
